# =============================================================================
# Recall Browser Extension - Environment Variables
# =============================================================================
#
# Copy this file to .env.local and fill in the values for your environment.
# These variables are used by Vite during build time and are embedded in the
# extension bundle.
#
# IMPORTANT: Never commit .env.local or any file containing real credentials!
#

# -----------------------------------------------------------------------------
# API Configuration
# -----------------------------------------------------------------------------

# Base URL for the Recall API (without /api/v1 suffix)
# Development default: http://localhost:5080
# Production example: https://api.recall.example.com
VITE_API_BASE_URL=http://localhost:5080

# URL of the Recall web application (for side panel embedding)
# Development default: http://localhost:5173
# Production example: https://app.recall.example.com
VITE_WEB_APP_URL=http://localhost:5173

# -----------------------------------------------------------------------------
# Microsoft Entra External ID (CIAM) Configuration
# -----------------------------------------------------------------------------

# Entra External ID authority URL
# Format: https://<tenant-subdomain>.ciamlogin.com/<tenant-id>
# Example: https://recalldemo.ciamlogin.com/12345678-1234-1234-1234-123456789abc
# This is the OAuth 2.0 authorization server endpoint for your Entra tenant.
VITE_ENTRA_AUTHORITY=

# Entra client ID (Application ID) for the browser extension
# This is the unique identifier for your extension app registration in Entra.
# Format: 12345678-1234-1234-1234-123456789abc
VITE_ENTRA_CLIENT_ID=

# OAuth 2.0 redirect URL for the extension
# For Chrome extensions, this must use the chromiumapp.org format:
# https://<extension-id>.chromiumapp.org/<redirect-path>
# Example: https://abcdefghijklmnopqrstuvwxyz.chromiumapp.org/auth/callback
# Note: The extension ID is generated when you publish/load the extension.
VITE_ENTRA_REDIRECT_URL=

# API scope for requesting access tokens
# Format: api://<api-client-id>/access_as_user
# This scope must be exposed by your API app registration in Entra and
# pre-authorized for the extension client ID.
# Example: api://87654321-4321-4321-4321-cba987654321/access_as_user
VITE_API_SCOPE=

# -----------------------------------------------------------------------------
# Additional Notes
# -----------------------------------------------------------------------------
#
# 1. All VITE_* variables are accessible in the extension code via import.meta.env
# 2. The apiBaseUrl should NOT include the /api/v1 prefix - it's added by the API client
# 3. For local development with Aspire, use http://localhost:5080 for the API
# 4. The redirect URL must be registered in your Entra app registration's redirect URIs
# 5. Ensure the API scope is pre-authorized to avoid user consent prompts
#
