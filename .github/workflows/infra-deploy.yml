name: Deploy Infrastructure

on:
  workflow_dispatch:
    inputs:
      environment:
        description: Target environment
        required: true
        type: choice
        options:
          - dev
          - prod

permissions:
  id-token: write
  contents: read

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: ${{ inputs.environment }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Azure Login
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

      - name: Deploy Bicep
        uses: azure/arm-deploy@v2
        env:
          DOCUMENTDB_ADMIN_PASSWORD: ${{ secrets.DOCUMENTDB_ADMIN_PASSWORD }}
          AZUREAD_TENANT_ID: ${{ secrets.AZUREAD_TENANT_ID }}
          AZUREAD_API_CLIENT_ID: ${{ secrets.AZUREAD_API_CLIENT_ID }}
          AZUREAD_API_AUDIENCE: ${{ secrets.AZUREAD_API_AUDIENCE }}
        with:
          scope: subscription
          region: westeurope
          template: ./infra/main.bicep
          parameters: ./infra/parameters/${{ inputs.environment }}.bicepparam
          deploymentName: recall-${{ inputs.environment }}-${{ github.run_number }}
